using System;
class TrainingLab
{
    static void Main()
    {
        double w = double.Parse(Console.ReadLine());
        double h = double.Parse(Console.ReadLine());
        w *= 100;
        h *= 100;
        if ((300 <= h && w <= 10000) && (h <= w))
        {
            h -= 100;
            int heightTable = (int)h / 70;
            int wideTable = (int)w / 120;
            int multiplied = heightTable * wideTable;
            Console.WriteLine(multiplied - 3);
        }
    }
}
